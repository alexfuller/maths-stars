{
  "name": "maths-stars",
  "version": "1.0.0",
  "description": "Maths Stars — mental arithmetic practice webapp",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "npx serve ."
  }
}
